rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Dev / no-auth POS — replace with proper rules before production.
    match /orders/{document=**} {
      allow read, write: if true;
    }
  }
}
